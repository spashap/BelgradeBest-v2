# build output
dist/
.output/

# deps
node_modules/

# astro
.astro/

# env (secrets — never commit)
.env
.env.local
.env.production

# raw full-res hero source PNGs (kept locally for re-cropping; not deployed)
heroes-src/

# social syndication state (local run log; not deployed)
scripts/.syndicate-state.json

# os / editor
.DS_Store
Thumbs.db
.vercel
