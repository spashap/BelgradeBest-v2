Pavilion atlas: let the command bar wrap on desktop

On wide screens the sticky selector bar scrolled horizontally like on a
phone, so the sort segment and the live count sat off-screen. From 900px
the row now wraps instead of scrolling; phones keep the swipeable row.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PnoBgSeUqk1fGPRnNGfFoy
