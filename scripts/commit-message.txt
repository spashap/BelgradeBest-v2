Redesign /admin Analytics: high-tech dashboard with richer GA4 data

Replace the three plain GA4/GSC tables with a live operator dashboard. The page
stays server-rendered (prerender=false) so every load fetches live; adds a
date-range selector, a Refresh control, a "fetched at" UTC stamp, and a realtime
"active now" pill. Prioritizes browsing patterns, trends, and traffic sources.

- src/lib/admin/analytics.ts: add ga4Overview(days) — one batched pull (2x
  batchRunReports) returning headline KPIs with period-over-period deltas
  (visitors/sessions/views/engagement rate/avg engagement/views-per-session),
  a daily trend series, traffic channels, devices, geo, new-vs-returning,
  landing pages (sessions/avg-engage/bounce) and top pages (views/users/engage).
  Add ga4Realtime() for active-users-now. Both degrade gracefully like the
  existing helpers; existing ga4TopPages/GSC functions kept intact.
- src/pages/admin/analytics.astro: rebuilt UI in the dark admin shell —
  KPI stat-cards with up/down deltas, inline-SVG sparklines (no JS chart lib),
  realtime pulse pill, 7/28/90-day range segmented control, traffic-source/
  device/geo/new-vs-returning bar lists, landing-page + top-page browsing-
  pattern tables, retained Search Console queries/pages sections.

No new deps, no DB, public site untouched (admin-only, still noindex).

Co-Authored-By: Claude <noreply@anthropic.com>
