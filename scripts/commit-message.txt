Platform Phase 0: Expo participant tracker, countdown widget, badge, /for-businesses

First build phase of KB/platform/PLATFORM-PLAN.md (included, rev-b + Phase 0
marked done). Four link-building assets, all static:

Participant tracker (/expo-2027/tracker):
- New data master src/data/expo-participants.json (web-researched 2026-07-05,
  every entry sourced): official counter 139 international participants, 93
  publicly named countries with region + confirmation dates, growth timeline
  94 -> 117 -> 120 -> 137 -> 139, organiser expectations, sources, caveats.
- lib/expo-participants.ts (typed accessor, byRegion stats) + tracker page:
  stat tiles, timeline + region + full country tables, sources & method,
  cite-this note. JSON-LD: Article + Dataset + BreadcrumbList (new
  datasetSchema() in lib/schemas.ts).
- Open data endpoint /data/expo-2027-participants.json (same master,
  Dataset.distribution points at it).

Countdown widget (link magnet for anyone writing about the Expo):
- public/widgets/expo-countdown.js — self-contained embed (no cookies, no
  external requests), counts down to 15 May 2027, switches to days-left
  during the Expo; attribution link back is the license.
- /expo-2027/countdown — demo + copy-paste embed code + per-platform steps.

Badge + business landing:
- public/badges/featured-on-belgradebest.svg (Warm Editorial, inline colors
  by necessity — renders on third-party sites).
- /for-businesses — the platform pitch: free page, GBP website-field how-to,
  badge embed code, claim-by-email CTA (admin@belgradebest.com) until the
  Phase-2 self-serve claim flow ships.

Design-law library additions (globals.css): .c-stats/.c-stat tiles,
.c-data-table (also styles markdown tables inside .c-article — the medical
price tables were unstyled until now), .c-snippet + Snippet.astro copy-button
component.

Content: expo-2027/participants.md refreshed 137 -> 139 (official counter,
US confirmed 2026-05-11), cross-linked to the tracker throughout, lastUpdated
bumped; description/lede/FAQs updated to match.

Infra: astro.config.mjs sitemap lastmod for the three new pages (tracker
inherits expo-participants.json `updated`; countdown + for-businesses carry
hand-bumped dates — never BUILD_DATE).

Build verified: all three pages prerendered + in sitemap with real lastmod,
tracker renders 93 countries / 110 table rows, Dataset JSON-LD present,
badge/widget/data files in dist. Pre-existing (not from this change): the
glob-loader "Duplicate id participants" build warning also fires on the
previous commit.

Co-Authored-By: Claude <noreply@anthropic.com>
