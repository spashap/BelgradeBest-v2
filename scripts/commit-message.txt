Growth API: noindex headers on refusals; track the Facebook cover

Every /api/growth/* reply now carries the same Cache-Control: private, no-store
AND X-Robots-Tag: noindex, nofollow. Previously only successful responses set
X-Robots-Tag; the 401/503 refusals from authErrorResponse() omitted it, so the
API's own documentation ("every response carries…") was not quite true. This is
defence-in-depth only — /api/ is already disallowed in robots.txt and error
responses are not indexed — but the surface is now consistent whether a reply is
data or a refusal. A new test asserts both headers across 401, 503, 400 and two
200 responses.

public/FB-COVER.png (the Facebook Page cover, added 2026-09-06) is committed so
the social asset is version-controlled with the rest of the brand files. It is
served at /FB-COVER.png, is not linked from any page and is not in the sitemap.

growth/growth_changes.jsonl gains BB-CHG-0002 for both changes, per the
change-log rule in CLAUDE.md.

Tests 36 passing (Google mocked, no API quota). Build green. No public page,
template, content or tracking change; the sitemap is unchanged at 156 URLs.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_016GNmaZAPQgs98DiSbBvM7A
