Remove the Reddit account from the site (footer + sameAs)

Reddit banned the brand account, so the site must not point at it: the
Reddit URL is dropped from brand.sameAs (Organization JSON-LD) and the
footer social entry is set to visible:false (kept in site-config so it can
be re-enabled from the admin Navigation screen if the account comes back
or a new one is created). Facebook stays.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PnoBgSeUqk1fGPRnNGfFoy
